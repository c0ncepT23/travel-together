module.exports = function(api) {
    api.cache(true);
    return {
      presets: ['babel-preset-expo'],
      plugins: [
        'react-native-reanimated/plugin',
        // Any other plugins should come AFTER the Reanimated plugin
      ],
    };
  };